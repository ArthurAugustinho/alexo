# version control
.git
.gitignore

# env files (injetados em runtime via --env-file ou docker-compose)
.env*
!.env.example

# deps (reinstaladas no build)
node_modules

# next.js build output (gerado no estágio builder)
.next

# docker
docker-compose.yml
Dockerfile
.dockerignore

# drizzle
drizzle/meta

# uploads (montados via volume em produção)
public/uploads

# misc
*.md
*.log
.DS_Store
coverage
.vercel
*.tsbuildinfo
next-env.d.ts
