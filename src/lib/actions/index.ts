// Convention:
// - server actions live in src/lib/actions
// - each file groups actions by domain or bounded context
// - shared zod schemas stay outside "use server" files so client code can import them safely

export {};
