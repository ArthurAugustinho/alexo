"use client";

import { Button } from "@/components/ui/button";

const FinishOrderButton = () => {
  return (
    <Button disabled className="w-full rounded-full opacity-60" size="lg">
      Finalizar compra — Em breve disponível
    </Button>
  );
};

export default FinishOrderButton;
